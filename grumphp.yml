parameters:
    git_dir: .
    bin_dir: ./vendor/bin
    tasks:
      phpcs:
        standard: PSR2
        ignore_patterns:
          - "web/*"
          - "app/*"
      phpunit:
        config_file: phpunit.xml
      yamllint: ~
      xmllint: ~
      jsonlint: ~

